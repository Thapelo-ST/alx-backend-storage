// listing all the documents in the collection school

// connecting to server

const conn = new Mongo();

const db = conn.getDB("my_db");

const collection = db.getCollection("school");

// listing document

const cursur = collection.find();

while (cursur.hasNext()) {
	printjson(cursur.next());
}

